# coding: utf-8

"""
    Survey Engine MVP API

    Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 

    The version of the OpenAPI document: 1.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class SurveyThemeLayout(BaseModel):
    """
    SurveyThemeLayout
    """ # noqa: E501
    content_width: Optional[StrictStr] = Field(default=None, alias="contentWidth")
    header_style: Optional[StrictStr] = Field(default=None, alias="headerStyle")
    header_alignment: Optional[StrictStr] = Field(default=None, alias="headerAlignment")
    footer_style: Optional[StrictStr] = Field(default=None, alias="footerStyle")
    footer_alignment: Optional[StrictStr] = Field(default=None, alias="footerAlignment")
    section_style: Optional[StrictStr] = Field(default=None, alias="sectionStyle")
    question_card_style: Optional[StrictStr] = Field(default=None, alias="questionCardStyle")
    category_separator_style: Optional[StrictStr] = Field(default=None, alias="categorySeparatorStyle")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["contentWidth", "headerStyle", "headerAlignment", "footerStyle", "footerAlignment", "sectionStyle", "questionCardStyle", "categorySeparatorStyle"]

    @field_validator('content_width')
    def content_width_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['narrow', 'standard', 'wide']):
            raise ValueError("must be one of enum values ('narrow', 'standard', 'wide')")
        return value

    @field_validator('header_style')
    def header_style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['hero', 'compact', 'editorial', 'minimal']):
            raise ValueError("must be one of enum values ('hero', 'compact', 'editorial', 'minimal')")
        return value

    @field_validator('header_alignment')
    def header_alignment_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['left', 'center', 'right']):
            raise ValueError("must be one of enum values ('left', 'center', 'right')")
        return value

    @field_validator('footer_style')
    def footer_style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['minimal', 'support', 'legal', 'editorial']):
            raise ValueError("must be one of enum values ('minimal', 'support', 'legal', 'editorial')")
        return value

    @field_validator('footer_alignment')
    def footer_alignment_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['left', 'center', 'right']):
            raise ValueError("must be one of enum values ('left', 'center', 'right')")
        return value

    @field_validator('section_style')
    def section_style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['panel', 'flat', 'editorial']):
            raise ValueError("must be one of enum values ('panel', 'flat', 'editorial')")
        return value

    @field_validator('question_card_style')
    def question_card_style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['soft', 'solid', 'minimal']):
            raise ValueError("must be one of enum values ('soft', 'solid', 'minimal')")
        return value

    @field_validator('category_separator_style')
    def category_separator_style_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['divider', 'chapter', 'minimal']):
            raise ValueError("must be one of enum values ('divider', 'chapter', 'minimal')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SurveyThemeLayout from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SurveyThemeLayout from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "contentWidth": obj.get("contentWidth"),
            "headerStyle": obj.get("headerStyle"),
            "headerAlignment": obj.get("headerAlignment"),
            "footerStyle": obj.get("footerStyle"),
            "footerAlignment": obj.get("footerAlignment"),
            "sectionStyle": obj.get("sectionStyle"),
            "questionCardStyle": obj.get("questionCardStyle"),
            "categorySeparatorStyle": obj.get("categorySeparatorStyle")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


