/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AuthConfigAudit,
  AuthProfileRequest,
  AuthProfileResponse,
  ErrorResponse,
  ProviderTemplateResponse,
  TokenValidationResult,
} from '../models/index';
import {
    AuthConfigAuditFromJSON,
    AuthConfigAuditToJSON,
    AuthProfileRequestFromJSON,
    AuthProfileRequestToJSON,
    AuthProfileResponseFromJSON,
    AuthProfileResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ProviderTemplateResponseFromJSON,
    ProviderTemplateResponseToJSON,
    TokenValidationResultFromJSON,
    TokenValidationResultToJSON,
} from '../models/index';

export interface CreateAuthProfileRequest {
    authProfileRequest: AuthProfileRequest;
}

export interface GetAuthProfileAuditRequest {
    id: string;
}

export interface GetAuthProfileByTenantRequest {
    tenantId: string;
}

export interface GetProviderTemplateRequest {
    providerCode: string;
}

export interface RotateAuthProfileKeyRequest {
    id: string;
}

export interface UpdateAuthProfileRequest {
    id: string;
    authProfileRequest: AuthProfileRequest;
}

export interface ValidateResponderTokenRequest {
    tenantId: string;
    body?: string;
}

/**
 * 
 */
export class AuthProfilesApi extends runtime.BaseAPI {

    /**
     * Creates request options for createAuthProfile without sending the request
     */
    async createAuthProfileRequestOpts(requestParameters: CreateAuthProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['authProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'authProfileRequest',
                'Required parameter "authProfileRequest" was null or undefined when calling createAuthProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/profiles`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: AuthProfileRequestToJSON(requestParameters['authProfileRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Private campaign authentication must be configured once per tenant so every campaign can reuse a consistent trust policy.  What this endpoint does: It creates a tenant auth profile containing auth mode, trust metadata, claim mappings, fallback policy, and OIDC/signing settings.  How this endpoint does it: The service validates tenant scope, enforces mapping rules (including required respondentId mapping), applies defaults (such as OIDC scope defaults), persists profile + mappings, and writes audit events. 
     * Create tenant responder-auth profile
     */
    async createAuthProfileRaw(requestParameters: CreateAuthProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthProfileResponse>> {
        const requestOptions = await this.createAuthProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Private campaign authentication must be configured once per tenant so every campaign can reuse a consistent trust policy.  What this endpoint does: It creates a tenant auth profile containing auth mode, trust metadata, claim mappings, fallback policy, and OIDC/signing settings.  How this endpoint does it: The service validates tenant scope, enforces mapping rules (including required respondentId mapping), applies defaults (such as OIDC scope defaults), persists profile + mappings, and writes audit events. 
     * Create tenant responder-auth profile
     */
    async createAuthProfile(requestParameters: CreateAuthProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthProfileResponse> {
        const response = await this.createAuthProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAuthProfileAudit without sending the request
     */
    async getAuthProfileAuditRequestOpts(requestParameters: GetAuthProfileAuditRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getAuthProfileAudit().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/profiles/{id}/audit`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Security-sensitive configuration changes require traceability for compliance and troubleshooting.  What this endpoint does: It returns ordered audit entries for an auth profile including actor and before/after values.  How this endpoint does it: The repository queries audit rows by auth_profile_id in descending timestamp order and returns serialized audit objects. 
     * Get auth profile audit history
     */
    async getAuthProfileAuditRaw(requestParameters: GetAuthProfileAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<AuthConfigAudit>>> {
        const requestOptions = await this.getAuthProfileAuditRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(AuthConfigAuditFromJSON));
    }

    /**
     * Why this endpoint is needed: Security-sensitive configuration changes require traceability for compliance and troubleshooting.  What this endpoint does: It returns ordered audit entries for an auth profile including actor and before/after values.  How this endpoint does it: The repository queries audit rows by auth_profile_id in descending timestamp order and returns serialized audit objects. 
     * Get auth profile audit history
     */
    async getAuthProfileAudit(requestParameters: GetAuthProfileAuditRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<AuthConfigAudit>> {
        const response = await this.getAuthProfileAuditRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getAuthProfileByTenant without sending the request
     */
    async getAuthProfileByTenantRequestOpts(requestParameters: GetAuthProfileByTenantRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['tenantId'] == null) {
            throw new runtime.RequiredError(
                'tenantId',
                'Required parameter "tenantId" was null or undefined when calling getAuthProfileByTenant().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/profiles/tenant/{tenantId}`;
        urlPath = urlPath.replace(`{${"tenantId"}}`, encodeURIComponent(String(requestParameters['tenantId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Operators need an easy way to verify effective auth configuration before launching or troubleshooting private campaigns.  What this endpoint does: It returns the tenant\'s auth profile and claim mapping configuration.  How this endpoint does it: Tenant access checks are performed, then profile data is loaded and mapped to response DTO. 
     * Get auth profile for a tenant
     */
    async getAuthProfileByTenantRaw(requestParameters: GetAuthProfileByTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthProfileResponse>> {
        const requestOptions = await this.getAuthProfileByTenantRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Operators need an easy way to verify effective auth configuration before launching or troubleshooting private campaigns.  What this endpoint does: It returns the tenant\'s auth profile and claim mapping configuration.  How this endpoint does it: Tenant access checks are performed, then profile data is loaded and mapped to response DTO. 
     * Get auth profile for a tenant
     */
    async getAuthProfileByTenant(requestParameters: GetAuthProfileByTenantRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthProfileResponse> {
        const response = await this.getAuthProfileByTenantRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getProviderTemplate without sending the request
     */
    async getProviderTemplateRequestOpts(requestParameters: GetProviderTemplateRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['providerCode'] == null) {
            throw new runtime.RequiredError(
                'providerCode',
                'Required parameter "providerCode" was null or undefined when calling getProviderTemplate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/providers/templates/{providerCode}`;
        urlPath = urlPath.replace(`{${"providerCode"}}`, encodeURIComponent(String(requestParameters['providerCode'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Setup forms often require provider-specific field guidance and defaults rather than a full template catalog.  What this endpoint does: It returns one template by provider code (for example OKTA, AUTH0, AZURE_AD, KEYCLOAK).  How this endpoint does it: The service validates provider code input, resolves the template from in-memory catalog, and returns structured metadata. 
     * Get one provider setup template
     */
    async getProviderTemplateRaw(requestParameters: GetProviderTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProviderTemplateResponse>> {
        const requestOptions = await this.getProviderTemplateRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ProviderTemplateResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Setup forms often require provider-specific field guidance and defaults rather than a full template catalog.  What this endpoint does: It returns one template by provider code (for example OKTA, AUTH0, AZURE_AD, KEYCLOAK).  How this endpoint does it: The service validates provider code input, resolves the template from in-memory catalog, and returns structured metadata. 
     * Get one provider setup template
     */
    async getProviderTemplate(requestParameters: GetProviderTemplateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProviderTemplateResponse> {
        const response = await this.getProviderTemplateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listProviderTemplates without sending the request
     */
    async listProviderTemplatesRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/providers/templates`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Tenant onboarding is faster and less error-prone when common IdP providers have pre-defined setup templates.  What this endpoint does: It returns template metadata for supported providers, including default scopes, required config fields, and default claim mapping suggestions.  How this endpoint does it: A template service returns static curated provider definitions consumed by UI and SDK onboarding flows. 
     * List built-in provider setup templates
     */
    async listProviderTemplatesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<ProviderTemplateResponse>>> {
        const requestOptions = await this.listProviderTemplatesRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(ProviderTemplateResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Tenant onboarding is faster and less error-prone when common IdP providers have pre-defined setup templates.  What this endpoint does: It returns template metadata for supported providers, including default scopes, required config fields, and default claim mapping suggestions.  How this endpoint does it: A template service returns static curated provider definitions consumed by UI and SDK onboarding flows. 
     * List built-in provider setup templates
     */
    async listProviderTemplates(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<ProviderTemplateResponse>> {
        const response = await this.listProviderTemplatesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for rotateAuthProfileKey without sending the request
     */
    async rotateAuthProfileKeyRequestOpts(requestParameters: RotateAuthProfileKeyRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling rotateAuthProfileKey().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/profiles/{id}/rotate-key`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Signing key version rotation is required for security hygiene and incident response readiness.  What this endpoint does: It increments the active key version for a tenant auth profile.  How this endpoint does it: The service updates active key version, persists change, and creates an audit record describing the rotation action. 
     * Rotate auth profile key version
     */
    async rotateAuthProfileKeyRaw(requestParameters: RotateAuthProfileKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthProfileResponse>> {
        const requestOptions = await this.rotateAuthProfileKeyRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Signing key version rotation is required for security hygiene and incident response readiness.  What this endpoint does: It increments the active key version for a tenant auth profile.  How this endpoint does it: The service updates active key version, persists change, and creates an audit record describing the rotation action. 
     * Rotate auth profile key version
     */
    async rotateAuthProfileKey(requestParameters: RotateAuthProfileKeyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthProfileResponse> {
        const response = await this.rotateAuthProfileKeyRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateAuthProfile without sending the request
     */
    async updateAuthProfileRequestOpts(requestParameters: UpdateAuthProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateAuthProfile().'
            );
        }

        if (requestParameters['authProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'authProfileRequest',
                'Required parameter "authProfileRequest" was null or undefined when calling updateAuthProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/auth/profiles/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: AuthProfileRequestToJSON(requestParameters['authProfileRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Identity provider details and claim models change over time; profile updates must be supported without rebuilding tenants.  What this endpoint does: It updates auth profile trust settings, claim mappings, scope behavior, and fallback policies.  How this endpoint does it: The service validates tenant access and mapping constraints, applies secure defaults where required, persists profile changes, and records before/after snapshots in auth audit. 
     * Update tenant responder-auth profile
     */
    async updateAuthProfileRaw(requestParameters: UpdateAuthProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AuthProfileResponse>> {
        const requestOptions = await this.updateAuthProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AuthProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Identity provider details and claim models change over time; profile updates must be supported without rebuilding tenants.  What this endpoint does: It updates auth profile trust settings, claim mappings, scope behavior, and fallback policies.  How this endpoint does it: The service validates tenant access and mapping constraints, applies secure defaults where required, persists profile changes, and records before/after snapshots in auth audit. 
     * Update tenant responder-auth profile
     */
    async updateAuthProfile(requestParameters: UpdateAuthProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AuthProfileResponse> {
        const response = await this.updateAuthProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for validateResponderToken without sending the request
     */
    async validateResponderTokenRequestOpts(requestParameters: ValidateResponderTokenRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['tenantId'] == null) {
            throw new runtime.RequiredError(
                'tenantId',
                'Required parameter "tenantId" was null or undefined when calling validateResponderToken().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'text/plain';


        let urlPath = `/api/v1/auth/validate/{tenantId}`;
        urlPath = urlPath.replace(`{${"tenantId"}}`, encodeURIComponent(String(requestParameters['tenantId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        };
    }

    /**
     * Why this endpoint is needed: Integrations and runtime checks require an explicit token-validation endpoint that applies tenant-specific trust policy.  What this endpoint does: It validates responder token input according to configured auth mode and claim-mapping rules, then returns standardized success/failure payload.  How this endpoint does it: Service loads tenant auth profile, routes validation by mode (public/signed/OIDC external trust), applies cryptographic and claim checks, and returns TokenValidationResult. 
     * Validate responder token against tenant auth policy
     */
    async validateResponderTokenRaw(requestParameters: ValidateResponderTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<TokenValidationResult>> {
        const requestOptions = await this.validateResponderTokenRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => TokenValidationResultFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Integrations and runtime checks require an explicit token-validation endpoint that applies tenant-specific trust policy.  What this endpoint does: It validates responder token input according to configured auth mode and claim-mapping rules, then returns standardized success/failure payload.  How this endpoint does it: Service loads tenant auth profile, routes validation by mode (public/signed/OIDC external trust), applies cryptographic and claim checks, and returns TokenValidationResult. 
     * Validate responder token against tenant auth policy
     */
    async validateResponderToken(requestParameters: ValidateResponderTokenRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<TokenValidationResult> {
        const response = await this.validateResponderTokenRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
