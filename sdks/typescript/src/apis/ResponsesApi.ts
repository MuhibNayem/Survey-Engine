/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CampaignAnalytics,
  ComparisonAnalyticsResponse,
  ComparisonRequest,
  ErrorResponse,
  FullCampaignAnalyticsResponse,
  ReopenRequest,
  ResponseSubmissionRequest,
  SurveyResponseResponse,
} from '../models/index';
import {
    CampaignAnalyticsFromJSON,
    CampaignAnalyticsToJSON,
    ComparisonAnalyticsResponseFromJSON,
    ComparisonAnalyticsResponseToJSON,
    ComparisonRequestFromJSON,
    ComparisonRequestToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    FullCampaignAnalyticsResponseFromJSON,
    FullCampaignAnalyticsResponseToJSON,
    ReopenRequestFromJSON,
    ReopenRequestToJSON,
    ResponseSubmissionRequestFromJSON,
    ResponseSubmissionRequestToJSON,
    SurveyResponseResponseFromJSON,
    SurveyResponseResponseToJSON,
} from '../models/index';

export interface CompareSegmentsRequest {
    campaignId: string;
    body: ComparisonRequest;
}

export interface GetCampaignAnalyticsRequest {
    campaignId: string;
}

export interface GetFullReportRequest {
    campaignId: string;
}

export interface GetResponseRequest {
    id: string;
}

export interface ListResponsesByCampaignRequest {
    campaignId: string;
}

export interface LockResponseRequest {
    id: string;
}

export interface ReopenResponseRequest {
    id: string;
    reopenRequest: ReopenRequest;
}

export interface SubmitResponseRequest {
    responseSubmissionRequest: ResponseSubmissionRequest;
}

/**
 * 
 */
export class ResponsesApi extends runtime.BaseAPI {

    /**
     * Creates request options for compareSegments without sending the request
     */
    async compareSegmentsRequestOpts(requestParameters: CompareSegmentsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling compareSegments().'
            );
        }

        if (requestParameters['body'] == null) {
            throw new runtime.RequiredError(
                'body',
                'Required parameter "body" was null or undefined when calling compareSegments().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/analytics/campaigns/{campaignId}/compare`;
        urlPath = urlPath.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['body'] as any,
        };
    }

    /**
     * Why this endpoint is needed: Platform users need side-by-side comparative analytics of demographic segments.  What this endpoint does: Multiplexes the /full-report generation across up to 5 concurrent demographic segments. 
     * Compare multiple analytics segments
     */
    async compareSegmentsRaw(requestParameters: CompareSegmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ComparisonAnalyticsResponse>> {
        const requestOptions = await this.compareSegmentsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ComparisonAnalyticsResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Platform users need side-by-side comparative analytics of demographic segments.  What this endpoint does: Multiplexes the /full-report generation across up to 5 concurrent demographic segments. 
     * Compare multiple analytics segments
     */
    async compareSegments(requestParameters: CompareSegmentsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ComparisonAnalyticsResponse> {
        const response = await this.compareSegmentsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCampaignAnalytics without sending the request
     */
    async getCampaignAnalyticsRequestOpts(requestParameters: GetCampaignAnalyticsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling getCampaignAnalytics().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/responses/analytics/{campaignId}`;
        urlPath = urlPath.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign operators need near-real-time aggregate visibility without reading raw response payloads.  What this endpoint does: It returns aggregate counts and completion rate metrics for a campaign.  How this endpoint does it: Analytics service runs campaign-scoped aggregate queries under tenant context and returns CampaignAnalytics DTO. 
     * Get aggregated analytics for a campaign
     */
    async getCampaignAnalyticsRaw(requestParameters: GetCampaignAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignAnalytics>> {
        const requestOptions = await this.getCampaignAnalyticsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignAnalyticsFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaign operators need near-real-time aggregate visibility without reading raw response payloads.  What this endpoint does: It returns aggregate counts and completion rate metrics for a campaign.  How this endpoint does it: Analytics service runs campaign-scoped aggregate queries under tenant context and returns CampaignAnalytics DTO. 
     * Get aggregated analytics for a campaign
     */
    async getCampaignAnalytics(requestParameters: GetCampaignAnalyticsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignAnalytics> {
        const response = await this.getCampaignAnalyticsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getFullReport without sending the request
     */
    async getFullReportRequestOpts(requestParameters: GetFullReportRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling getFullReport().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/analytics/campaigns/{campaignId}/full-report`;
        urlPath = urlPath.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: The dashboard needs an aggregated snapshot of campaign performance, score curves, and individual question breakdowns.  What this endpoint does: Returns all analytics metrics for a single segment (or the entire campaign). 
     * Get full analytics report with metadata filters
     */
    async getFullReportRaw(requestParameters: GetFullReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FullCampaignAnalyticsResponse>> {
        const requestOptions = await this.getFullReportRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FullCampaignAnalyticsResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: The dashboard needs an aggregated snapshot of campaign performance, score curves, and individual question breakdowns.  What this endpoint does: Returns all analytics metrics for a single segment (or the entire campaign). 
     * Get full analytics report with metadata filters
     */
    async getFullReport(requestParameters: GetFullReportRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FullCampaignAnalyticsResponse> {
        const response = await this.getFullReportRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getResponse without sending the request
     */
    async getResponseRequestOpts(requestParameters: GetResponseRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getResponse().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/responses/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Support and operational teams need direct access to a specific response record for verification and exception handling.  What this endpoint does: It returns one response and its answer details.  How this endpoint does it: Response service resolves response by id under tenant context and maps nested answer records to DTO. 
     * Get one response by id
     */
    async getResponseRaw(requestParameters: GetResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.getResponseRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Support and operational teams need direct access to a specific response record for verification and exception handling.  What this endpoint does: It returns one response and its answer details.  How this endpoint does it: Response service resolves response by id under tenant context and maps nested answer records to DTO. 
     * Get one response by id
     */
    async getResponse(requestParameters: GetResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse> {
        const response = await this.getResponseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listResponsesByCampaign without sending the request
     */
    async listResponsesByCampaignRequestOpts(requestParameters: ListResponsesByCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling listResponsesByCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/responses/campaign/{campaignId}`;
        urlPath = urlPath.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign managers need a scoped list view of responses for review, quality checks, and manual workflows.  What this endpoint does: It returns campaign-scoped response list under tenant ownership checks.  How this endpoint does it: The service validates campaign accessibility under tenant context and fetches mapped response rows by campaign id. 
     * List responses for a campaign
     */
    async listResponsesByCampaignRaw(requestParameters: ListResponsesByCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<SurveyResponseResponse>>> {
        const requestOptions = await this.listResponsesByCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(SurveyResponseResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Campaign managers need a scoped list view of responses for review, quality checks, and manual workflows.  What this endpoint does: It returns campaign-scoped response list under tenant ownership checks.  How this endpoint does it: The service validates campaign accessibility under tenant context and fetches mapped response rows by campaign id. 
     * List responses for a campaign
     */
    async listResponsesByCampaign(requestParameters: ListResponsesByCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<SurveyResponseResponse>> {
        const response = await this.listResponsesByCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for lockResponse without sending the request
     */
    async lockResponseRequestOpts(requestParameters: LockResponseRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling lockResponse().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/responses/{id}/lock`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Although responses auto-lock on submit, operations teams may need explicit lock controls for exceptional workflows.  What this endpoint does: It changes response state to locked and records lock timestamp.  How this endpoint does it: Locking service validates current state and tenant ownership, applies status mutation, and returns updated response. 
     * Lock a response manually
     */
    async lockResponseRaw(requestParameters: LockResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.lockResponseRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Although responses auto-lock on submit, operations teams may need explicit lock controls for exceptional workflows.  What this endpoint does: It changes response state to locked and records lock timestamp.  How this endpoint does it: Locking service validates current state and tenant ownership, applies status mutation, and returns updated response. 
     * Lock a response manually
     */
    async lockResponse(requestParameters: LockResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse> {
        const response = await this.lockResponseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for reopenResponse without sending the request
     */
    async reopenResponseRequestOpts(requestParameters: ReopenResponseRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling reopenResponse().'
            );
        }

        if (requestParameters['reopenRequest'] == null) {
            throw new runtime.RequiredError(
                'reopenRequest',
                'Required parameter "reopenRequest" was null or undefined when calling reopenResponse().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/responses/{id}/reopen`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ReopenRequestToJSON(requestParameters['reopenRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Real operations require a controlled exception path for corrections after lock.  What this endpoint does: It reopens a previously locked response and captures reason/window metadata.  How this endpoint does it: Locking service validates state transitions and reason constraints, updates response status, and returns reopened response record. If campaign close date has passed, close-time lock enforcement may lock it again. 
     * Reopen a locked response with reason
     */
    async reopenResponseRaw(requestParameters: ReopenResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.reopenResponseRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Real operations require a controlled exception path for corrections after lock.  What this endpoint does: It reopens a previously locked response and captures reason/window metadata.  How this endpoint does it: Locking service validates state transitions and reason constraints, updates response status, and returns reopened response record. If campaign close date has passed, close-time lock enforcement may lock it again. 
     * Reopen a locked response with reason
     */
    async reopenResponse(requestParameters: ReopenResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse> {
        const response = await this.reopenResponseRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for submitResponse without sending the request
     */
    async submitResponseRequestOpts(requestParameters: SubmitResponseRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['responseSubmissionRequest'] == null) {
            throw new runtime.RequiredError(
                'responseSubmissionRequest',
                'Required parameter "responseSubmissionRequest" was null or undefined when calling submitResponse().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/responses`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResponseSubmissionRequestToJSON(requestParameters['responseSubmissionRequest']),
        };
    }

    /**
     * Why this endpoint is needed: This is the central data-ingestion endpoint where responder participation is captured.  What this endpoint does: It accepts answer payload for a campaign, enforces campaign status and runtime controls, enforces private-auth requirements where applicable, validates against pinned snapshot versions, auto-calculates weighted score when default profile exists, stores response and answers, then auto-locks response.  How this endpoint does it: The service performs campaign lookup, access-mode enforcement (public or private), credential validation (responderToken or responderAccessCode for private), settings checks (quota/restrictions/timeouts), snapshot-based answer validation/scoring, persistence, and status transition to LOCKED before returning SurveyResponseResponse. 
     * Submit survey response (public and private campaign entry)
     */
    async submitResponseRaw(requestParameters: SubmitResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.submitResponseRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: This is the central data-ingestion endpoint where responder participation is captured.  What this endpoint does: It accepts answer payload for a campaign, enforces campaign status and runtime controls, enforces private-auth requirements where applicable, validates against pinned snapshot versions, auto-calculates weighted score when default profile exists, stores response and answers, then auto-locks response.  How this endpoint does it: The service performs campaign lookup, access-mode enforcement (public or private), credential validation (responderToken or responderAccessCode for private), settings checks (quota/restrictions/timeouts), snapshot-based answer validation/scoring, persistence, and status transition to LOCKED before returning SurveyResponseResponse. 
     * Submit survey response (public and private campaign entry)
     */
    async submitResponse(requestParameters: SubmitResponseRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse> {
        const response = await this.submitResponseRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
