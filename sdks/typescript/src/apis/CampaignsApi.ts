/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CampaignPreviewResponse,
  CampaignRequest,
  CampaignResponse,
  CampaignSettingsRequest,
  CampaignSettingsResponse,
  DistributionChannelResponse,
  ErrorResponse,
  ResponderSessionStatusResponse,
  ResponseDraftLookupRequest,
  ResponseSubmissionRequest,
  SurveyResponseResponse,
} from '../models/index';
import {
    CampaignPreviewResponseFromJSON,
    CampaignPreviewResponseToJSON,
    CampaignRequestFromJSON,
    CampaignRequestToJSON,
    CampaignResponseFromJSON,
    CampaignResponseToJSON,
    CampaignSettingsRequestFromJSON,
    CampaignSettingsRequestToJSON,
    CampaignSettingsResponseFromJSON,
    CampaignSettingsResponseToJSON,
    DistributionChannelResponseFromJSON,
    DistributionChannelResponseToJSON,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ResponderSessionStatusResponseFromJSON,
    ResponderSessionStatusResponseToJSON,
    ResponseDraftLookupRequestFromJSON,
    ResponseDraftLookupRequestToJSON,
    ResponseSubmissionRequestFromJSON,
    ResponseSubmissionRequestToJSON,
    SurveyResponseResponseFromJSON,
    SurveyResponseResponseToJSON,
} from '../models/index';

export interface ActivateCampaignRequest {
    id: string;
}

export interface CreateCampaignRequest {
    campaignRequest: CampaignRequest;
}

export interface DeactivateCampaignRequest {
    id: string;
}

export interface GenerateCampaignChannelsRequest {
    id: string;
}

export interface GetCampaignRequest {
    id: string;
}

export interface GetCampaignPreviewRequest {
    id: string;
}

export interface GetCampaignSettingsRequest {
    id: string;
}

export interface GetPublicCampaignPreviewRequest {
    id: string;
}

export interface GetResponderSessionStatusRequest {
    id: string;
}

export interface ListCampaignChannelsRequest {
    id: string;
}

export interface LoadPublicDraftRequest {
    id: string;
    responseDraftLookupRequest: ResponseDraftLookupRequest;
}

export interface LogoutResponderSessionRequest {
    id: string;
}

export interface SavePublicDraftRequest {
    id: string;
    responseSubmissionRequest: ResponseSubmissionRequest;
}

export interface UpdateCampaignRequest {
    id: string;
    campaignRequest: CampaignRequest;
}

export interface UpdateCampaignSettingsRequest {
    id: string;
    campaignSettingsRequest: CampaignSettingsRequest;
}

/**
 * 
 */
export class CampaignsApi extends runtime.BaseAPI {

    /**
     * Creates request options for activateCampaign without sending the request
     */
    async activateCampaignRequestOpts(requestParameters: ActivateCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling activateCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/activate`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Activation is a controlled launch gate. It prevents collecting responses from improperly prepared campaigns.  What this endpoint does: It transitions a campaign into active state when preconditions pass, links latest survey snapshot, and upserts campaign default weight profile from pinned category weights.  How this endpoint does it: The service verifies linked survey lifecycle requirements (must be PUBLISHED), loads latest snapshot, upserts default weight profile, updates campaign status to ACTIVE, and returns campaign response. 
     * Activate campaign to accept responses
     */
    async activateCampaignRaw(requestParameters: ActivateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResponse>> {
        const requestOptions = await this.activateCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Activation is a controlled launch gate. It prevents collecting responses from improperly prepared campaigns.  What this endpoint does: It transitions a campaign into active state when preconditions pass, links latest survey snapshot, and upserts campaign default weight profile from pinned category weights.  How this endpoint does it: The service verifies linked survey lifecycle requirements (must be PUBLISHED), loads latest snapshot, upserts default weight profile, updates campaign status to ACTIVE, and returns campaign response. 
     * Activate campaign to accept responses
     */
    async activateCampaign(requestParameters: ActivateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResponse> {
        const response = await this.activateCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createCampaign without sending the request
     */
    async createCampaignRequestOpts(requestParameters: CreateCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignRequest'] == null) {
            throw new runtime.RequiredError(
                'campaignRequest',
                'Required parameter "campaignRequest" was null or undefined when calling createCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignRequestToJSON(requestParameters['campaignRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Campaigns are the executable delivery unit for collecting responses from a specific survey snapshot and runtime policy set.  What this endpoint does: It creates a tenant-scoped campaign with name, survey binding, description, and access mode (PUBLIC/PRIVATE).  How this endpoint does it: The service validates survey reference and tenant scope, normalizes deprecated auth mode values, persists campaign state, and returns CampaignResponse. 
     * Create a campaign from a survey
     */
    async createCampaignRaw(requestParameters: CreateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResponse>> {
        const requestOptions = await this.createCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaigns are the executable delivery unit for collecting responses from a specific survey snapshot and runtime policy set.  What this endpoint does: It creates a tenant-scoped campaign with name, survey binding, description, and access mode (PUBLIC/PRIVATE).  How this endpoint does it: The service validates survey reference and tenant scope, normalizes deprecated auth mode values, persists campaign state, and returns CampaignResponse. 
     * Create a campaign from a survey
     */
    async createCampaign(requestParameters: CreateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResponse> {
        const response = await this.createCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deactivateCampaign without sending the request
     */
    async deactivateCampaignRequestOpts(requestParameters: DeactivateCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deactivateCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Retiring campaign availability should not erase historical response evidence.  What this endpoint does: It marks campaign inactive.  How this endpoint does it: Tenant-scoped campaign lookup is followed by status/active-state mutation and no-content completion. 
     * Deactivate campaign
     */
    async deactivateCampaignRaw(requestParameters: DeactivateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deactivateCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Why this endpoint is needed: Retiring campaign availability should not erase historical response evidence.  What this endpoint does: It marks campaign inactive.  How this endpoint does it: Tenant-scoped campaign lookup is followed by status/active-state mutation and no-content completion. 
     * Deactivate campaign
     */
    async deactivateCampaign(requestParameters: DeactivateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deactivateCampaignRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for generateCampaignChannels without sending the request
     */
    async generateCampaignChannelsRequestOpts(requestParameters: GenerateCampaignChannelsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling generateCampaignChannels().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/distribute`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Operations teams need standardized distribution artifacts (links and embed formats) to launch outreach quickly.  What this endpoint does: It generates and stores channel records such as public/private links and embed snippets.  How this endpoint does it: Distribution service builds channel values from campaign context, persists channel rows, and returns generated list. 
     * Generate distribution channels for a campaign
     */
    async generateCampaignChannelsRaw(requestParameters: GenerateCampaignChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DistributionChannelResponse>>> {
        const requestOptions = await this.generateCampaignChannelsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DistributionChannelResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Operations teams need standardized distribution artifacts (links and embed formats) to launch outreach quickly.  What this endpoint does: It generates and stores channel records such as public/private links and embed snippets.  How this endpoint does it: Distribution service builds channel values from campaign context, persists channel rows, and returns generated list. 
     * Generate distribution channels for a campaign
     */
    async generateCampaignChannels(requestParameters: GenerateCampaignChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DistributionChannelResponse>> {
        const response = await this.generateCampaignChannelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCampaign without sending the request
     */
    async getCampaignRequestOpts(requestParameters: GetCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign detail retrieval is required for settings updates, monitoring, and support troubleshooting.  What this endpoint does: It returns one campaign record with current status and metadata.  How this endpoint does it: The service resolves campaign by tenant and id and returns mapped DTO fields. 
     * Get campaign by id
     */
    async getCampaignRaw(requestParameters: GetCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResponse>> {
        const requestOptions = await this.getCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaign detail retrieval is required for settings updates, monitoring, and support troubleshooting.  What this endpoint does: It returns one campaign record with current status and metadata.  How this endpoint does it: The service resolves campaign by tenant and id and returns mapped DTO fields. 
     * Get campaign by id
     */
    async getCampaign(requestParameters: GetCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResponse> {
        const response = await this.getCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCampaignPreview without sending the request
     */
    async getCampaignPreviewRequestOpts(requestParameters: GetCampaignPreviewRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCampaignPreview().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/preview`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign owners need a pre-launch visual/data validation endpoint to verify header/footer/questions and responder UX flags.  What this endpoint does: It assembles campaign, survey, and settings into a responder-facing preview model for admin users.  How this endpoint does it: Campaign service resolves campaign/settings/survey in tenant scope, maps each page/question with pinned version context, and returns CampaignPreviewResponse. 
     * Get admin preview payload for a campaign
     */
    async getCampaignPreviewRaw(requestParameters: GetCampaignPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignPreviewResponse>> {
        const requestOptions = await this.getCampaignPreviewRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignPreviewResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaign owners need a pre-launch visual/data validation endpoint to verify header/footer/questions and responder UX flags.  What this endpoint does: It assembles campaign, survey, and settings into a responder-facing preview model for admin users.  How this endpoint does it: Campaign service resolves campaign/settings/survey in tenant scope, maps each page/question with pinned version context, and returns CampaignPreviewResponse. 
     * Get admin preview payload for a campaign
     */
    async getCampaignPreview(requestParameters: GetCampaignPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignPreviewResponse> {
        const response = await this.getCampaignPreviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getCampaignSettings without sending the request
     */
    async getCampaignSettingsRequestOpts(requestParameters: GetCampaignSettingsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getCampaignSettings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/settings`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Operations and UI flows require a read endpoint for persisted runtime controls before edit/save cycles.  What this endpoint does: It returns the current campaign settings object.  How this endpoint does it: Campaign service validates tenant-scoped campaign visibility and returns mapped CampaignSettingsResponse. 
     * Get campaign runtime settings
     */
    async getCampaignSettingsRaw(requestParameters: GetCampaignSettingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignSettingsResponse>> {
        const requestOptions = await this.getCampaignSettingsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignSettingsResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Operations and UI flows require a read endpoint for persisted runtime controls before edit/save cycles.  What this endpoint does: It returns the current campaign settings object.  How this endpoint does it: Campaign service validates tenant-scoped campaign visibility and returns mapped CampaignSettingsResponse. 
     * Get campaign runtime settings
     */
    async getCampaignSettings(requestParameters: GetCampaignSettingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignSettingsResponse> {
        const response = await this.getCampaignSettingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getPublicCampaignPreview without sending the request
     */
    async getPublicCampaignPreviewRequestOpts(requestParameters: GetPublicCampaignPreviewRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getPublicCampaignPreview().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/public/campaigns/{id}/preview`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Responder runtime needs a non-admin endpoint to load campaign form structure for public/private submission flows.  What this endpoint does: It returns preview payload for campaigns currently active.  How this endpoint does it: Service resolves campaign/settings/survey and returns CampaignPreviewResponse when campaign is active; access mode enforcement for private campaigns happens on submit/auth endpoints. 
     * Get responder-facing preview payload (public endpoint)
     */
    async getPublicCampaignPreviewRaw(requestParameters: GetPublicCampaignPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignPreviewResponse>> {
        const requestOptions = await this.getPublicCampaignPreviewRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignPreviewResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Responder runtime needs a non-admin endpoint to load campaign form structure for public/private submission flows.  What this endpoint does: It returns preview payload for campaigns currently active.  How this endpoint does it: Service resolves campaign/settings/survey and returns CampaignPreviewResponse when campaign is active; access mode enforcement for private campaigns happens on submit/auth endpoints. 
     * Get responder-facing preview payload (public endpoint)
     */
    async getPublicCampaignPreview(requestParameters: GetPublicCampaignPreviewRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignPreviewResponse> {
        const response = await this.getPublicCampaignPreviewRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getResponderSessionStatus without sending the request
     */
    async getResponderSessionStatusRequestOpts(requestParameters: GetResponderSessionStatusRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getResponderSessionStatus().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/public/campaigns/{id}/auth/session`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Private responder runtime needs a non-destructive way to detect whether an authenticated responder session already exists after SSO redirect or page refresh.  What this endpoint does: It returns whether a valid responder session cookie is currently active for the target private campaign.  How this endpoint does it: The controller resolves the campaign, validates that it is private, reads the responder session cookie, and returns a simple authenticated/email payload. 
     * Get current private responder session status for a campaign
     */
    async getResponderSessionStatusRaw(requestParameters: GetResponderSessionStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ResponderSessionStatusResponse>> {
        const requestOptions = await this.getResponderSessionStatusRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ResponderSessionStatusResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Private responder runtime needs a non-destructive way to detect whether an authenticated responder session already exists after SSO redirect or page refresh.  What this endpoint does: It returns whether a valid responder session cookie is currently active for the target private campaign.  How this endpoint does it: The controller resolves the campaign, validates that it is private, reads the responder session cookie, and returns a simple authenticated/email payload. 
     * Get current private responder session status for a campaign
     */
    async getResponderSessionStatus(requestParameters: GetResponderSessionStatusRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ResponderSessionStatusResponse> {
        const response = await this.getResponderSessionStatusRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listCampaignChannels without sending the request
     */
    async listCampaignChannelsRequestOpts(requestParameters: ListCampaignChannelsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling listCampaignChannels().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/channels`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Teams often need to re-fetch channel assets after initial generation for reminders and omnichannel delivery.  What this endpoint does: It returns all persisted distribution channels for the campaign.  How this endpoint does it: Distribution service reads campaign channel rows under tenant scope and maps to response DTOs. 
     * List generated channels for a campaign
     */
    async listCampaignChannelsRaw(requestParameters: ListCampaignChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<DistributionChannelResponse>>> {
        const requestOptions = await this.listCampaignChannelsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(DistributionChannelResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Teams often need to re-fetch channel assets after initial generation for reminders and omnichannel delivery.  What this endpoint does: It returns all persisted distribution channels for the campaign.  How this endpoint does it: Distribution service reads campaign channel rows under tenant scope and maps to response DTOs. 
     * List generated channels for a campaign
     */
    async listCampaignChannels(requestParameters: ListCampaignChannelsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<DistributionChannelResponse>> {
        const response = await this.listCampaignChannelsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listCampaigns without sending the request
     */
    async listCampaignsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign operations teams need a dashboard view of active and manageable campaigns.  What this endpoint does: It returns campaigns under authenticated tenant scope.  How this endpoint does it: Campaign service executes tenant-scoped retrieval and response mapping. 
     * List active campaigns
     */
    async listCampaignsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<CampaignResponse>>> {
        const requestOptions = await this.listCampaignsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(CampaignResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Campaign operations teams need a dashboard view of active and manageable campaigns.  What this endpoint does: It returns campaigns under authenticated tenant scope.  How this endpoint does it: Campaign service executes tenant-scoped retrieval and response mapping. 
     * List active campaigns
     */
    async listCampaigns(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<CampaignResponse>> {
        const response = await this.listCampaignsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for loadPublicDraft without sending the request
     */
    async loadPublicDraftRequestOpts(requestParameters: LoadPublicDraftRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling loadPublicDraft().'
            );
        }

        if (requestParameters['responseDraftLookupRequest'] == null) {
            throw new runtime.RequiredError(
                'responseDraftLookupRequest',
                'Required parameter "responseDraftLookupRequest" was null or undefined when calling loadPublicDraft().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/public/campaigns/{id}/responses/draft/load`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResponseDraftLookupRequestToJSON(requestParameters['responseDraftLookupRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Responder runtime must restore saved survey state after refresh, return visit, or successful private SSO authentication.  What this endpoint does: It returns the matching in-progress draft when one exists for the supplied identity or response id.  How this endpoint does it: The service looks up the latest open response by explicit response id or responder identity, validates access mode, and returns 204 when no draft exists. 
     * Load an existing in-progress responder draft
     */
    async loadPublicDraftRaw(requestParameters: LoadPublicDraftRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.loadPublicDraftRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Responder runtime must restore saved survey state after refresh, return visit, or successful private SSO authentication.  What this endpoint does: It returns the matching in-progress draft when one exists for the supplied identity or response id.  How this endpoint does it: The service looks up the latest open response by explicit response id or responder identity, validates access mode, and returns 204 when no draft exists. 
     * Load an existing in-progress responder draft
     */
    async loadPublicDraft(requestParameters: LoadPublicDraftRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse | null | undefined > {
        const response = await this.loadPublicDraftRaw(requestParameters, initOverrides);
        switch (response.raw.status) {
            case 200:
                return await response.value();
            case 204:
                return null;
            default:
                return await response.value();
        }
    }

    /**
     * Creates request options for logoutResponderSession without sending the request
     */
    async logoutResponderSessionRequestOpts(requestParameters: LogoutResponderSessionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling logoutResponderSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/public/campaigns/{id}/auth/logout`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Private survey sessions require explicit sign-out on shared or managed devices without relying only on expiry.  What this endpoint does: It revokes the current responder session and clears the responder session cookie.  How this endpoint does it: The controller resolves the campaign, revokes the matching responder session for the current cookie, and returns a no-content response with a clearing cookie. 
     * Revoke the current private responder session for a campaign
     */
    async logoutResponderSessionRaw(requestParameters: LogoutResponderSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.logoutResponderSessionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Why this endpoint is needed: Private survey sessions require explicit sign-out on shared or managed devices without relying only on expiry.  What this endpoint does: It revokes the current responder session and clears the responder session cookie.  How this endpoint does it: The controller resolves the campaign, revokes the matching responder session for the current cookie, and returns a no-content response with a clearing cookie. 
     * Revoke the current private responder session for a campaign
     */
    async logoutResponderSession(requestParameters: LogoutResponderSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.logoutResponderSessionRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for savePublicDraft without sending the request
     */
    async savePublicDraftRequestOpts(requestParameters: SavePublicDraftRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling savePublicDraft().'
            );
        }

        if (requestParameters['responseSubmissionRequest'] == null) {
            throw new runtime.RequiredError(
                'responseSubmissionRequest',
                'Required parameter "responseSubmissionRequest" was null or undefined when calling savePublicDraft().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/public/campaigns/{id}/responses/draft`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: ResponseSubmissionRequestToJSON(requestParameters['responseSubmissionRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Responders need an interruption-safe draft path so multi-page surveys can be resumed later without losing answers or metadata.  What this endpoint does: It creates a new IN_PROGRESS response or updates an existing draft response for the target campaign.  How this endpoint does it: The service validates campaign access rules, upserts the response row, merges answers by question, persists respondent metadata, and returns the current draft state. 
     * Create or update an in-progress responder draft
     */
    async savePublicDraftRaw(requestParameters: SavePublicDraftRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SurveyResponseResponse>> {
        const requestOptions = await this.savePublicDraftRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SurveyResponseResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Responders need an interruption-safe draft path so multi-page surveys can be resumed later without losing answers or metadata.  What this endpoint does: It creates a new IN_PROGRESS response or updates an existing draft response for the target campaign.  How this endpoint does it: The service validates campaign access rules, upserts the response row, merges answers by question, persists respondent metadata, and returns the current draft state. 
     * Create or update an in-progress responder draft
     */
    async savePublicDraft(requestParameters: SavePublicDraftRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SurveyResponseResponse> {
        const response = await this.savePublicDraftRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateCampaign without sending the request
     */
    async updateCampaignRequestOpts(requestParameters: UpdateCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateCampaign().'
            );
        }

        if (requestParameters['campaignRequest'] == null) {
            throw new runtime.RequiredError(
                'campaignRequest',
                'Required parameter "campaignRequest" was null or undefined when calling updateCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignRequestToJSON(requestParameters['campaignRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Campaigns require controlled edits to operational metadata and associations.  What this endpoint does: It updates campaign request fields while preserving lifecycle constraints.  How this endpoint does it: Service validates ownership and allowed update semantics, then persists and returns updated campaign data. 
     * Update campaign metadata
     */
    async updateCampaignRaw(requestParameters: UpdateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResponse>> {
        const requestOptions = await this.updateCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaigns require controlled edits to operational metadata and associations.  What this endpoint does: It updates campaign request fields while preserving lifecycle constraints.  How this endpoint does it: Service validates ownership and allowed update semantics, then persists and returns updated campaign data. 
     * Update campaign metadata
     */
    async updateCampaign(requestParameters: UpdateCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResponse> {
        const response = await this.updateCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateCampaignSettings without sending the request
     */
    async updateCampaignSettingsRequestOpts(requestParameters: UpdateCampaignSettingsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateCampaignSettings().'
            );
        }

        if (requestParameters['campaignSettingsRequest'] == null) {
            throw new runtime.RequiredError(
                'campaignSettingsRequest',
                'Required parameter "campaignSettingsRequest" was null or undefined when calling updateCampaignSettings().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/campaigns/{id}/settings`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: CampaignSettingsRequestToJSON(requestParameters['campaignSettingsRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Campaign runtime behavior (quota, timeout, restrictions, UI toggles, metadata capture) must be adjustable independently from core campaign identity.  What this endpoint does: It stores runtime policy controls that are enforced when responders submit answers. If closeDate is set to now/past, open responses are auto-locked immediately.  How this endpoint does it: The service loads target campaign by tenant, merges settings payload, persists state, and returns updated campaign context. 
     * Update campaign runtime settings
     */
    async updateCampaignSettingsRaw(requestParameters: UpdateCampaignSettingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CampaignResponse>> {
        const requestOptions = await this.updateCampaignSettingsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CampaignResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Campaign runtime behavior (quota, timeout, restrictions, UI toggles, metadata capture) must be adjustable independently from core campaign identity.  What this endpoint does: It stores runtime policy controls that are enforced when responders submit answers. If closeDate is set to now/past, open responses are auto-locked immediately.  How this endpoint does it: The service loads target campaign by tenant, merges settings payload, persists state, and returns updated campaign context. 
     * Update campaign runtime settings
     */
    async updateCampaignSettings(requestParameters: UpdateCampaignSettingsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CampaignResponse> {
        const response = await this.updateCampaignSettingsRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
