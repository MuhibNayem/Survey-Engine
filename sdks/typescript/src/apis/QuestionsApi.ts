/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  QuestionRequest,
  QuestionResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    QuestionRequestFromJSON,
    QuestionRequestToJSON,
    QuestionResponseFromJSON,
    QuestionResponseToJSON,
} from '../models/index';

export interface CreateQuestionRequest {
    questionRequest: QuestionRequest;
}

export interface DeactivateQuestionRequest {
    id: string;
}

export interface GetQuestionRequest {
    id: string;
}

export interface UpdateQuestionRequest {
    id: string;
    questionRequest: QuestionRequest;
}

/**
 * 
 */
export class QuestionsApi extends runtime.BaseAPI {

    /**
     * Creates request options for createQuestion without sending the request
     */
    async createQuestionRequestOpts(requestParameters: CreateQuestionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['questionRequest'] == null) {
            throw new runtime.RequiredError(
                'questionRequest',
                'Required parameter "questionRequest" was null or undefined when calling createQuestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/questions`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: QuestionRequestToJSON(requestParameters['questionRequest']),
        };
    }

    /**
     * Why this endpoint is needed: The question bank is the foundation of reusable survey content. Teams need a way to add standardized question assets once and reuse them across many surveys.  What this endpoint does: It creates a question with text, type, and scoring metadata and stores version context used by downstream snapshots.  How this endpoint does it: The service validates required fields and max score constraints, applies tenant scoping from auth context, persists the question entity, and returns a QuestionResponse containing metadata and version pointers. 
     * Create a reusable question
     */
    async createQuestionRaw(requestParameters: CreateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QuestionResponse>> {
        const requestOptions = await this.createQuestionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QuestionResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: The question bank is the foundation of reusable survey content. Teams need a way to add standardized question assets once and reuse them across many surveys.  What this endpoint does: It creates a question with text, type, and scoring metadata and stores version context used by downstream snapshots.  How this endpoint does it: The service validates required fields and max score constraints, applies tenant scoping from auth context, persists the question entity, and returns a QuestionResponse containing metadata and version pointers. 
     * Create a reusable question
     */
    async createQuestion(requestParameters: CreateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QuestionResponse> {
        const response = await this.createQuestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deactivateQuestion without sending the request
     */
    async deactivateQuestionRequestOpts(requestParameters: DeactivateQuestionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deactivateQuestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/questions/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Historical integrity is important, so hard-deletes are avoided while obsolete questions must be removed from active authoring lists.  What this endpoint does: It marks the target question as inactive.  How this endpoint does it: The service resolves the question by tenant context, updates active status, and returns no-content on success. 
     * Deactivate a question
     */
    async deactivateQuestionRaw(requestParameters: DeactivateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deactivateQuestionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Why this endpoint is needed: Historical integrity is important, so hard-deletes are avoided while obsolete questions must be removed from active authoring lists.  What this endpoint does: It marks the target question as inactive.  How this endpoint does it: The service resolves the question by tenant context, updates active status, and returns no-content on success. 
     * Deactivate a question
     */
    async deactivateQuestion(requestParameters: DeactivateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deactivateQuestionRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getQuestion without sending the request
     */
    async getQuestionRequestOpts(requestParameters: GetQuestionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getQuestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/questions/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Detailed question retrieval is required for edit forms, review pages, and diagnostics.  What this endpoint does: It returns one question record by identifier under tenant access control.  How this endpoint does it: The service resolves the record by ID with tenant-scoped checks and maps the result to a QuestionResponse DTO. 
     * Get one question by id
     */
    async getQuestionRaw(requestParameters: GetQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QuestionResponse>> {
        const requestOptions = await this.getQuestionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QuestionResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Detailed question retrieval is required for edit forms, review pages, and diagnostics.  What this endpoint does: It returns one question record by identifier under tenant access control.  How this endpoint does it: The service resolves the record by ID with tenant-scoped checks and maps the result to a QuestionResponse DTO. 
     * Get one question by id
     */
    async getQuestion(requestParameters: GetQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QuestionResponse> {
        const response = await this.getQuestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listQuestions without sending the request
     */
    async listQuestionsRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/questions`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Authoring UIs require fast retrieval of available question inventory during survey design.  What this endpoint does: It returns active, tenant-scoped question records currently available for composition.  How this endpoint does it: The question service uses tenant-aware repository filtering and maps entity rows into response DTOs for list rendering. 
     * List active questions for current tenant
     */
    async listQuestionsRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<QuestionResponse>>> {
        const requestOptions = await this.listQuestionsRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(QuestionResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Authoring UIs require fast retrieval of available question inventory during survey design.  What this endpoint does: It returns active, tenant-scoped question records currently available for composition.  How this endpoint does it: The question service uses tenant-aware repository filtering and maps entity rows into response DTOs for list rendering. 
     * List active questions for current tenant
     */
    async listQuestions(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<QuestionResponse>> {
        const response = await this.listQuestionsRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateQuestion without sending the request
     */
    async updateQuestionRequestOpts(requestParameters: UpdateQuestionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateQuestion().'
            );
        }

        if (requestParameters['questionRequest'] == null) {
            throw new runtime.RequiredError(
                'questionRequest',
                'Required parameter "questionRequest" was null or undefined when calling updateQuestion().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/questions/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: QuestionRequestToJSON(requestParameters['questionRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Content quality evolves over time and questions require controlled updates.  What this endpoint does: It updates question fields and maintains version continuity used by snapshot-based references.  How this endpoint does it: The service validates payload constraints, checks tenant ownership, persists changes, and returns updated question data. 
     * Update an existing question
     */
    async updateQuestionRaw(requestParameters: UpdateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<QuestionResponse>> {
        const requestOptions = await this.updateQuestionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => QuestionResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Content quality evolves over time and questions require controlled updates.  What this endpoint does: It updates question fields and maintains version continuity used by snapshot-based references.  How this endpoint does it: The service validates payload constraints, checks tenant ownership, persists changes, and returns updated question data. 
     * Update an existing question
     */
    async updateQuestion(requestParameters: UpdateQuestionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<QuestionResponse> {
        const response = await this.updateQuestionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
