/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  ScoreResult,
  WeightProfileRequest,
  WeightProfileResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    ScoreResultFromJSON,
    ScoreResultToJSON,
    WeightProfileRequestFromJSON,
    WeightProfileRequestToJSON,
    WeightProfileResponseFromJSON,
    WeightProfileResponseToJSON,
} from '../models/index';

export interface CalculateWeightedScoreRequest {
    profileId: string;
    requestBody: { [key: string]: number; };
}

export interface CreateWeightProfileRequest {
    weightProfileRequest: WeightProfileRequest;
}

export interface DeactivateWeightProfileRequest {
    id: string;
}

export interface GetWeightProfileRequest {
    id: string;
}

export interface ListWeightProfilesByCampaignRequest {
    campaignId: string;
}

export interface UpdateWeightProfileRequest {
    id: string;
    weightProfileRequest: WeightProfileRequest;
}

export interface ValidateWeightProfileRequest {
    id: string;
}

/**
 * 
 */
export class ScoringApi extends runtime.BaseAPI {

    /**
     * Creates request options for calculateWeightedScore without sending the request
     */
    async calculateWeightedScoreRequestOpts(requestParameters: CalculateWeightedScoreRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['profileId'] == null) {
            throw new runtime.RequiredError(
                'profileId',
                'Required parameter "profileId" was null or undefined when calling calculateWeightedScore().'
            );
        }

        if (requestParameters['requestBody'] == null) {
            throw new runtime.RequiredError(
                'requestBody',
                'Required parameter "requestBody" was null or undefined when calling calculateWeightedScore().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/calculate/{profileId}`;
        urlPath = urlPath.replace(`{${"profileId"}}`, encodeURIComponent(String(requestParameters['profileId'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['requestBody'],
        };
    }

    /**
     * Why this endpoint is needed: Consumers need deterministic score output from raw category data and configured business weighting policy.  What this endpoint does: It computes normalized category contributions and returns a total weighted score breakdown.  How this endpoint does it: Scoring engine loads profile weights, validates inputs, computes per-category normalized and weighted values, and returns ScoreResult with category details. 
     * Calculate weighted score from raw category scores
     */
    async calculateWeightedScoreRaw(requestParameters: CalculateWeightedScoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ScoreResult>> {
        const requestOptions = await this.calculateWeightedScoreRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ScoreResultFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Consumers need deterministic score output from raw category data and configured business weighting policy.  What this endpoint does: It computes normalized category contributions and returns a total weighted score breakdown.  How this endpoint does it: Scoring engine loads profile weights, validates inputs, computes per-category normalized and weighted values, and returns ScoreResult with category details. 
     * Calculate weighted score from raw category scores
     */
    async calculateWeightedScore(requestParameters: CalculateWeightedScoreRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ScoreResult> {
        const response = await this.calculateWeightedScoreRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for createWeightProfile without sending the request
     */
    async createWeightProfileRequestOpts(requestParameters: CreateWeightProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['weightProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'weightProfileRequest',
                'Required parameter "weightProfileRequest" was null or undefined when calling createWeightProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: WeightProfileRequestToJSON(requestParameters['weightProfileRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Score computation requires explicit category weighting definitions linked to campaign context.  What this endpoint does: It creates a weight profile that defines how raw category scores contribute to total score.  How this endpoint does it: Service validates campaign scope and category-weight payload, persists profile and nested weights, and returns profile representation. 
     * Create scoring weight profile
     */
    async createWeightProfileRaw(requestParameters: CreateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WeightProfileResponse>> {
        const requestOptions = await this.createWeightProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WeightProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Score computation requires explicit category weighting definitions linked to campaign context.  What this endpoint does: It creates a weight profile that defines how raw category scores contribute to total score.  How this endpoint does it: Service validates campaign scope and category-weight payload, persists profile and nested weights, and returns profile representation. 
     * Create scoring weight profile
     */
    async createWeightProfile(requestParameters: CreateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WeightProfileResponse> {
        const response = await this.createWeightProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deactivateWeightProfile without sending the request
     */
    async deactivateWeightProfileRequestOpts(requestParameters: DeactivateWeightProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deactivateWeightProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Obsolete profiles must be retired while preserving historical result integrity.  What this endpoint does: It marks profile inactive.  How this endpoint does it: Tenant-scoped lookup followed by active-state mutation and no-content response. 
     * Deactivate scoring profile
     */
    async deactivateWeightProfileRaw(requestParameters: DeactivateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deactivateWeightProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Why this endpoint is needed: Obsolete profiles must be retired while preserving historical result integrity.  What this endpoint does: It marks profile inactive.  How this endpoint does it: Tenant-scoped lookup followed by active-state mutation and no-content response. 
     * Deactivate scoring profile
     */
    async deactivateWeightProfile(requestParameters: DeactivateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deactivateWeightProfileRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getWeightProfile without sending the request
     */
    async getWeightProfileRequestOpts(requestParameters: GetWeightProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getWeightProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Teams need profile-level visibility for verification and lifecycle updates.  What this endpoint does: It returns one weight profile including category weights.  How this endpoint does it: Service loads profile by id under tenant context and maps to DTO. 
     * Get one scoring profile by id
     */
    async getWeightProfileRaw(requestParameters: GetWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WeightProfileResponse>> {
        const requestOptions = await this.getWeightProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WeightProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Teams need profile-level visibility for verification and lifecycle updates.  What this endpoint does: It returns one weight profile including category weights.  How this endpoint does it: Service loads profile by id under tenant context and maps to DTO. 
     * Get one scoring profile by id
     */
    async getWeightProfile(requestParameters: GetWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WeightProfileResponse> {
        const response = await this.getWeightProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listWeightProfilesByCampaign without sending the request
     */
    async listWeightProfilesByCampaignRequestOpts(requestParameters: ListWeightProfilesByCampaignRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['campaignId'] == null) {
            throw new runtime.RequiredError(
                'campaignId',
                'Required parameter "campaignId" was null or undefined when calling listWeightProfilesByCampaign().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles/campaign/{campaignId}`;
        urlPath = urlPath.replace(`{${"campaignId"}}`, encodeURIComponent(String(requestParameters['campaignId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Campaign-level scoring operations need discoverability of attached profiles.  What this endpoint does: It returns all active/in-scope profiles linked to the specified campaign.  How this endpoint does it: Service validates campaign access by tenant and fetches profile rows filtered by campaign id. 
     * List scoring profiles for campaign
     */
    async listWeightProfilesByCampaignRaw(requestParameters: ListWeightProfilesByCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<WeightProfileResponse>>> {
        const requestOptions = await this.listWeightProfilesByCampaignRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(WeightProfileResponseFromJSON));
    }

    /**
     * Why this endpoint is needed: Campaign-level scoring operations need discoverability of attached profiles.  What this endpoint does: It returns all active/in-scope profiles linked to the specified campaign.  How this endpoint does it: Service validates campaign access by tenant and fetches profile rows filtered by campaign id. 
     * List scoring profiles for campaign
     */
    async listWeightProfilesByCampaign(requestParameters: ListWeightProfilesByCampaignRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<WeightProfileResponse>> {
        const response = await this.listWeightProfilesByCampaignRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateWeightProfile without sending the request
     */
    async updateWeightProfileRequestOpts(requestParameters: UpdateWeightProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateWeightProfile().'
            );
        }

        if (requestParameters['weightProfileRequest'] == null) {
            throw new runtime.RequiredError(
                'weightProfileRequest',
                'Required parameter "weightProfileRequest" was null or undefined when calling updateWeightProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles/{id}`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PUT',
            headers: headerParameters,
            query: queryParameters,
            body: WeightProfileRequestToJSON(requestParameters['weightProfileRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Scoring rubrics evolve and profiles require controlled updates.  What this endpoint does: It updates profile metadata and category-weight assignments.  How this endpoint does it: Service validates payload and tenant ownership, persists new values, and returns updated profile DTO. 
     * Update scoring profile
     */
    async updateWeightProfileRaw(requestParameters: UpdateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<WeightProfileResponse>> {
        const requestOptions = await this.updateWeightProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => WeightProfileResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Scoring rubrics evolve and profiles require controlled updates.  What this endpoint does: It updates profile metadata and category-weight assignments.  How this endpoint does it: Service validates payload and tenant ownership, persists new values, and returns updated profile DTO. 
     * Update scoring profile
     */
    async updateWeightProfile(requestParameters: UpdateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<WeightProfileResponse> {
        const response = await this.updateWeightProfileRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for validateWeightProfile without sending the request
     */
    async validateWeightProfileRequestOpts(requestParameters: ValidateWeightProfileRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling validateWeightProfile().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/scoring/profiles/{id}/validate`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Invalid weighting definitions can produce misleading score outputs; explicit pre-check endpoint reduces operational mistakes.  What this endpoint does: It validates profile constraints (including total weight sum rule).  How this endpoint does it: Service computes profile totals and throws deterministic business error when constraints are violated. 
     * Validate category weight sum for a profile
     */
    async validateWeightProfileRaw(requestParameters: ValidateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.validateWeightProfileRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Why this endpoint is needed: Invalid weighting definitions can produce misleading score outputs; explicit pre-check endpoint reduces operational mistakes.  What this endpoint does: It validates profile constraints (including total weight sum rule).  How this endpoint does it: Service computes profile totals and throws deterministic business error when constraints are violated. 
     * Validate category weight sum for a profile
     */
    async validateWeightProfile(requestParameters: ValidateWeightProfileRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.validateWeightProfileRaw(requestParameters, initOverrides);
    }

}
