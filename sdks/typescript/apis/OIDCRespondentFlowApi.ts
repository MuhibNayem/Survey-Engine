/* tslint:disable */
/* eslint-disable */
/**
 * Survey Engine MVP API
 * Comprehensive API contract for the Survey Engine MVP.  This specification is written for product, implementation, and integration teams. Every endpoint description intentionally includes: 1) why the endpoint is needed, 2) what the endpoint does, 3) how the endpoint works inside the platform lifecycle.  Security model summary: - Admin APIs accept engine-issued JWT via HttpOnly access_token cookie (primary) or Bearer header (fallback). - Headless clients can use explicit token-mode auth endpoints under /api/v1/admin/auth/token/_*. - Admin sessions are maintained with refresh-token cookie rotation. - Browser cookie mode exposes CSRF bootstrap endpoint at /api/v1/admin/auth/csrf. - Responder submission endpoint is public at transport level, but private campaigns enforce tenant-scoped responder authentication in business logic. 
 *
 * The version of the OpenAPI document: 1.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  OidcCallbackResponse,
  OidcStartRequest,
  OidcStartResponse,
} from '../models/index';
import {
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
    OidcCallbackResponseFromJSON,
    OidcCallbackResponseToJSON,
    OidcStartRequestFromJSON,
    OidcStartRequestToJSON,
    OidcStartResponseFromJSON,
    OidcStartResponseToJSON,
} from '../models/index';

export interface CompleteRespondentOidcRequest {
    state: string;
    code: string;
}

export interface StartRespondentOidcRequest {
    oidcStartRequest: OidcStartRequest;
}

/**
 * 
 */
export class OIDCRespondentFlowApi extends runtime.BaseAPI {

    /**
     * Creates request options for completeRespondentOidc without sending the request
     */
    async completeRespondentOidcRequestOpts(requestParameters: CompleteRespondentOidcRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['state'] == null) {
            throw new runtime.RequiredError(
                'state',
                'Required parameter "state" was null or undefined when calling completeRespondentOidc().'
            );
        }

        if (requestParameters['code'] == null) {
            throw new runtime.RequiredError(
                'code',
                'Required parameter "code" was null or undefined when calling completeRespondentOidc().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['state'] != null) {
            queryParameters['state'] = requestParameters['state'];
        }

        if (requestParameters['code'] != null) {
            queryParameters['code'] = requestParameters['code'];
        }

        const headerParameters: runtime.HTTPHeaders = {};


        let urlPath = `/api/v1/auth/respondent/oidc/callback`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Why this endpoint is needed: Authorization-code OIDC requires callback handling to exchange code, verify trust, and convert external auth into internal responder access state.  What this endpoint does: It validates callback state/code, exchanges token at IdP, validates token claims, and issues one-time responder access code.  How this endpoint does it: OIDC state record is checked for expiry and one-time use, token exchange is executed via configured metadata, token validation service verifies claims/mappings, responder access code row is persisted, and either JSON response or redirect response is returned. 
     * Complete OIDC callback and issue one-time responder access code
     */
    async completeRespondentOidcRaw(requestParameters: CompleteRespondentOidcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OidcCallbackResponse>> {
        const requestOptions = await this.completeRespondentOidcRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OidcCallbackResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Authorization-code OIDC requires callback handling to exchange code, verify trust, and convert external auth into internal responder access state.  What this endpoint does: It validates callback state/code, exchanges token at IdP, validates token claims, and issues one-time responder access code.  How this endpoint does it: OIDC state record is checked for expiry and one-time use, token exchange is executed via configured metadata, token validation service verifies claims/mappings, responder access code row is persisted, and either JSON response or redirect response is returned. 
     * Complete OIDC callback and issue one-time responder access code
     */
    async completeRespondentOidc(requestParameters: CompleteRespondentOidcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OidcCallbackResponse> {
        const response = await this.completeRespondentOidcRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for startRespondentOidc without sending the request
     */
    async startRespondentOidcRequestOpts(requestParameters: StartRespondentOidcRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['oidcStartRequest'] == null) {
            throw new runtime.RequiredError(
                'oidcStartRequest',
                'Required parameter "oidcStartRequest" was null or undefined when calling startRespondentOidc().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';


        let urlPath = `/api/v1/auth/respondent/oidc/start`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: OidcStartRequestToJSON(requestParameters['oidcStartRequest']),
        };
    }

    /**
     * Why this endpoint is needed: Private campaigns need a secure and tenant-aware way to initiate responder SSO without exposing platform-internal logic.  What this endpoint does: It validates campaign + tenant eligibility for OIDC flow, creates one-time state/nonce context, and returns IdP authorization URL details.  How this endpoint does it: The OIDC service checks campaign auth mode and tenant auth profile mode, fetches OIDC metadata, stores state with expiry, composes authorization URL with scopes/state/nonce, and returns OidcStartResponse. 
     * Start private responder OIDC flow
     */
    async startRespondentOidcRaw(requestParameters: StartRespondentOidcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OidcStartResponse>> {
        const requestOptions = await this.startRespondentOidcRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => OidcStartResponseFromJSON(jsonValue));
    }

    /**
     * Why this endpoint is needed: Private campaigns need a secure and tenant-aware way to initiate responder SSO without exposing platform-internal logic.  What this endpoint does: It validates campaign + tenant eligibility for OIDC flow, creates one-time state/nonce context, and returns IdP authorization URL details.  How this endpoint does it: The OIDC service checks campaign auth mode and tenant auth profile mode, fetches OIDC metadata, stores state with expiry, composes authorization URL with scopes/state/nonce, and returns OidcStartResponse. 
     * Start private responder OIDC flow
     */
    async startRespondentOidc(requestParameters: StartRespondentOidcRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OidcStartResponse> {
        const response = await this.startRespondentOidcRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
