package com.bracits.surveyengine.campaign.entity;

/**
 * Types of data collection fields that can be configured per campaign.
 */
public enum DataCollectionFieldType {
    TEXT,
    EMAIL,
    PHONE,
    NUMBER,
    TEXTAREA
}
