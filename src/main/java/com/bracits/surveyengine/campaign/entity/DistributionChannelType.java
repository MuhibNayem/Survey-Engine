package com.bracits.surveyengine.campaign.entity;

/**
 * Distribution channel types per SRS §4.8.
 */
public enum DistributionChannelType {
    PUBLIC_LINK,
    PRIVATE_LINK,
    HTML_EMBED,
    WORDPRESS_EMBED,
    JS_EMBED,
    EMAIL
}
